{
  "name": "@ntnkit/example-messenger",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "prestart": "pnpm --filter @ntnkit/core run build && pnpm --filter @ntnkit/sdk --filter @ntnkit/sqlite run build",
    "start": "tsx index.ts"
  },
  "dependencies": {
    "@ntnkit/core": "workspace:*",
    "@ntnkit/sdk": "workspace:*",
    "@ntnkit/sqlite": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.4"
  }
}
